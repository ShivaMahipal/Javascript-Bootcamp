/**
 * Nested for loop😉
 */

for(let i=1 ; i<=10;i++){
    for(let j=1; j<=12; j++){
        let product = i * j;
        console.log(`${i} * ${j} = ${product}`);
    }
    console.log("-------------");
}